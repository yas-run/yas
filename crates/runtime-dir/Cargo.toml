[package]
name = "yas-runtime-dir"
version = "0.3.0"
edition.workspace = true
description = "Owner-only runtime directory selection shared by yas's IPC socket and compositor"
license.workspace = true
repository.workspace = true
homepage.workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
